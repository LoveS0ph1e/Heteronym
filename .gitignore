# ===========================================================
# Heteronym · 发布安全拦截
#
# 本仓库只应包含:LICENSE、README.md、.gitignore,以及人格 prompt md。
# 以下为防误纳的危险文件模式——它们可能含真实生产事故、用户行为记录
# 或受版权保护的内容,绝不可进入 git 历史。
#
# 注:主防线是「物理隔离」(本目录独立于工作目录,git 根本看不到那些
# 文件);本表是纵深防御,拦截日后可能被误拷进来的副本。
# ===========================================================

# —— 审计 / 重构 / 设计内部文档(含真实生产事故与用户行为)——
*-audit.md
*audit*.md
*-refactor-plan.md
*refactor*plan*
*REFACTOR*

# —— 历史副本 / 回滚点 / 旧版(含未脱敏的早期内容)——
archive/
*.bak
*.bak-*
*OLD*
*.OLD*

# —— 语料 / 训练集 / 原著(受版权保护,严禁分发)——
corpus/
*.pdf
*.jsonl
命运石之门*

# —— 部署 / 运维变体(含服务器与配置细节)——
*.deploy.txt
*.webui.txt
deploy/

# —— 草稿 / 临时 / 抓取产物 ——
_*
*draft*
*.tmp
temp/
ebooks/

# —— 系统 / 编辑器 ——
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*~
